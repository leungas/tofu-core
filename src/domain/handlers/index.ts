import { UserProvisionedListener } from './user.provisioned.handler';

/**
 * @const handlers
 * @description The handlers for listeners for event
 * @author Mark Leung <leungas@gmail.com>
 */
export const handlers = [UserProvisionedListener];
